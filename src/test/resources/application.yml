server:
  port: 8888
spring:
  application:
    name: julio

  datasource:
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    root: WARN
    com.financas.julio: INFO

api:
  security:
    token:
      secret: meu-segredo-super-secreto-e-longo-para-gerar-tokens
